import { DecimalPipe } from '@angular/common';
import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { AgentAction, AgentConfig, SimulationResponse } from '../../core/models/simulation.models';
import { SimulationService } from '../../core/services/simulation.service';
import { DecisionCardComponent } from '../../shared/components/decision-card.component';
import { EventCardComponent } from '../../shared/components/event-card.component';
import { HistoryTableComponent } from '../../shared/components/history-table.component';
import { MetricCardComponent } from '../../shared/components/metric-card.component';
import { PolicySummaryComponent } from '../../shared/components/policy-summary.component';
import { QTableComponent } from '../../shared/components/q-table.component';

@Component({
  selector: 'app-laboratory',
  standalone: true,
  imports: [FormsModule, DecimalPipe, EventCardComponent, DecisionCardComponent, MetricCardComponent, QTableComponent, PolicySummaryComponent, HistoryTableComponent],
  template: `<section class="page"><div class="hero lab-head"><div><span class="pill">Laboratorio interactivo</span><h1>Entrena el agente defensor</h1><p>Ejecuta eventos individuales para observar decisiones paso a paso, o entrena por lotes para ver cómo cambia la Q-table y la política aprendida.</p><div class="lab-steps"><span>1. Genera evento</span><span>2. Observa acción</span><span>3. Revisa recompensa</span><span>4. Compara Q-table</span></div></div><div class="actions"><button class="btn primary-action" (click)="step()" [disabled]="loading">{{loading ? 'Procesando...' : 'Ejecutar evento'}}</button>@for(n of [10,100,500,1000];track n){<button class="btn secondary" (click)="train(n)" [disabled]="loading">Entrenar {{n}}</button>}<button class="btn danger" (click)="reset()" [disabled]="loading">Resetear</button></div></div>@if(loading){<p class="notice loading panel">Operación en curso. El backend está actualizando el estado del agente.</p>}@if(error){<p class="notice error panel">{{error}}</p>}@if(state){<div class="grid two panel"><app-event-card [event]="state.currentResult?.event ?? null"/><app-decision-card [result]="state.currentResult"/></div><div class="grid three panel"><app-metric-card label="Episodios" [value]="state.metrics.episodes"/><app-metric-card label="Accuracy" [value]="(state.metrics.accuracy|number:'1.1-2')!"/><app-metric-card label="Recompensa media" [value]="(state.metrics.averageReward|number:'1.2-2')!"/></div><div class="grid two panel"><section class="card config-card"><h3>Hiperparámetros</h3><p class="muted">Cambia un parámetro y aplica la configuración antes de entrenar para comparar su efecto.</p><form class="grid two config-grid" (ngSubmit)="applyConfig()"><label>Alpha <input type="range" min="0" max="1" step="0.01" [(ngModel)]="config.alpha" name="alpha"><small>{{config.alpha}} · aprendizaje</small></label><label>Gamma <input type="range" min="0" max="1" step="0.01" [(ngModel)]="config.gamma" name="gamma"><small>{{config.gamma}} · recompensa futura</small></label><label>Epsilon <input type="range" min="0" max="1" step="0.01" [(ngModel)]="config.epsilon" name="epsilon"><small>{{config.epsilon}} · exploración</small></label><label>Anomalías <input type="range" min="0" max="1" step="0.01" [(ngModel)]="config.anomalyProbability" name="anomalyProbability"><small>{{config.anomalyProbability}} · probabilidad</small></label><label>Dificultad <select [(ngModel)]="config.environmentDifficulty" name="environmentDifficulty"><option>EASY</option><option>MEDIUM</option><option>HARD</option></select><small>ruido del entorno</small></label><button class="btn apply" type="submit" [disabled]="loading || !validConfig()">Aplicar configuración</button></form></section><section class="card"><h3>Distribución de acciones</h3><p class="muted">Ayuda a detectar si el agente bloquea demasiado, permite demasiado o usa monitoreo.</p><div class="action-row">@for(row of actionRows();track row.action){<p><span>{{row.action}}</span><b>{{row.count}}</b></p><div class="bar"><span [style.width.%]="row.percent"></span></div>}</div></section></div><div class="panel q-panel"><app-q-table [entries]="state.qTable"/></div><div class="grid two panel"><app-policy-summary [policy]="state.learnedPolicy"/><section class="card reward-card"><div class="section-head"><div><h3>Recompensas recientes</h3><p class="muted">La altura de cada barra resume la recompensa de un evento reciente.</p></div><span class="pill">últimos 40</span></div>@if(rewardBars().length > 1){<div class="spark" aria-label="Gráfico de recompensas recientes">@for(v of rewardBars();track $index){<span [style.height.%]="v"></span>}</div>}@else{<p class="empty-state">Ejecuta más eventos para visualizar la evolución de recompensas.</p>}</section></div><div class="panel"><app-history-table [history]="state.recentHistory"/></div>}</section>`,
  styles: [`.lab-head{display:grid;grid-template-columns:minmax(0,1.05fr) minmax(320px,.95fr);gap:24px;align-items:start}.actions{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;align-content:start}.primary-action{grid-column:1/-1}.panel{margin-top:20px}.lab-steps{display:flex;flex-wrap:wrap;gap:8px;margin-top:18px}.lab-steps span{border:1px solid var(--line);background:rgba(255,253,248,.72);border-radius:999px;padding:7px 10px;font-weight:900;color:var(--navy);font-size:.82rem}.config-card form{margin-top:12px}.config-card p{margin-top:4px}.config-grid{align-items:end}.apply{align-self:end}.action-row{display:grid;gap:8px;margin-top:14px}.action-row p{display:flex;justify-content:space-between;gap:12px;margin:0}.bar{height:9px;border-radius:999px;background:rgba(11,31,51,.08);overflow:hidden}.bar span{display:block;height:100%;background:linear-gradient(90deg,var(--teal),#8BC7BE);border-radius:inherit}.q-panel{display:block}.reward-card{min-height:250px}.section-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;margin-bottom:12px}.section-head h3{margin-bottom:4px}.spark{height:170px;display:flex;gap:5px;align-items:end;border:1px solid var(--line);border-radius:16px;padding:14px;background:linear-gradient(180deg,rgba(255,253,248,.92),rgba(20,118,110,.06))}.spark span{width:100%;min-width:5px;background:linear-gradient(180deg,var(--teal),#8BC7BE);border-radius:7px 7px 2px 2px;box-shadow:0 4px 12px rgba(20,118,110,.16)}@media(max-width:980px){.lab-head{grid-template-columns:1fr}.actions{grid-template-columns:repeat(3,minmax(0,1fr))}.primary-action{grid-column:auto}}@media(max-width:760px){.actions{grid-template-columns:1fr}.primary-action{grid-column:auto}.section-head{display:grid}}`]
})
export class LaboratoryComponent implements OnInit {
  state: SimulationResponse | null = null;
  loading = false;
  error = '';
  config: AgentConfig = { alpha: .2, gamma: .9, epsilon: .2, anomalyProbability: .3, environmentDifficulty: 'MEDIUM' };

  constructor(private readonly simulation: SimulationService) {}

  ngOnInit() { this.load(); }
  load() { this.run(() => this.simulation.status()); }
  step() { this.run(() => this.simulation.step()); }
  train(episodes: number) { this.run(() => this.simulation.train({ ...this.config, episodes })); }
  reset() { this.run(() => this.simulation.reset()); }
  applyConfig() { if (this.validConfig()) this.run(() => this.simulation.updateConfig(this.config)); }
  validConfig() { return ['alpha', 'gamma', 'epsilon', 'anomalyProbability'].every(k => { const v = this.config[k as keyof AgentConfig] as number; return v >= 0 && v <= 1; }); }
  actionRows() { const m = this.state?.metrics; const rows: { action: AgentAction; count: number }[] = [{ action: 'ALLOW', count: m?.allowedEvents ?? 0 }, { action: 'MONITOR', count: m?.monitoredEvents ?? 0 }, { action: 'BLOCK', count: m?.blockedEvents ?? 0 }]; const max = Math.max(1, ...rows.map(r => r.count)); return rows.map(r => ({ ...r, percent: r.count / max * 100 })); }
  rewardBars() { const h = this.state?.rewardHistory.slice(-40) ?? []; if (!h.length) return []; const min = Math.min(...h), max = Math.max(...h); return h.map(v => max === min ? 50 : 10 + ((v - min) / (max - min)) * 90); }
  private run(call: () => ReturnType<SimulationService['status']>) { this.loading = true; this.error = ''; call().subscribe({ next: s => { this.state = s; this.config = { ...s.config }; this.loading = false; }, error: () => { this.error = 'No se pudo conectar con el backend en http://localhost:8080/api.'; this.loading = false; } }); }
}
