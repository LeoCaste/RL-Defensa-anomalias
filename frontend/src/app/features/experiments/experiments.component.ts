import { DatePipe, DecimalPipe } from '@angular/common';
import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ExperimentResponse } from '../../core/models/experiment.models';
import { AgentConfig, SimulationResponse } from '../../core/models/simulation.models';
import { ExperimentService } from '../../core/services/experiment.service';
import { SimulationService } from '../../core/services/simulation.service';
import { MetricCardComponent } from '../../shared/components/metric-card.component';
import { PolicySummaryComponent } from '../../shared/components/policy-summary.component';
import { QTableComponent } from '../../shared/components/q-table.component';

@Component({
  selector: 'app-experiments',
  standalone: true,
  imports: [FormsModule, DatePipe, DecimalPipe, MetricCardComponent, QTableComponent, PolicySummaryComponent],
  template: `<section class="page"><div class="hero"><span class="pill">Experimentos</span><h1>Configuración y experimentos</h1><p>Ajusta los parámetros del agente, entrena ejecuciones y guarda resultados para comparar.</p></div><div class="grid two panel"><article class="card config-card"><div class="section-head"><div><h3>Parámetros del agente</h3><p class="muted">Estos valores se aplican al backend y quedan registrados al guardar experimentos.</p></div><span class="pill amber">Configuración</span></div><form class="config-grid" (ngSubmit)="applyConfig()"><label>Alpha <b>{{config.alpha|number:'1.2-2'}}</b><input type="range" min="0" max="1" step="0.05" [(ngModel)]="config.alpha" name="alpha"><small>Tasa de aprendizaje: más alta cambia la Q-table con mayor rapidez.</small></label><label>Gamma <b>{{config.gamma|number:'1.2-2'}}</b><input type="range" min="0" max="1" step="0.05" [(ngModel)]="config.gamma" name="gamma"><small>Descuento futuro: más alto valora recompensas posteriores.</small></label><label>Epsilon <b>{{config.epsilon|number:'1.2-2'}}</b><input type="range" min="0" max="1" step="0.05" [(ngModel)]="config.epsilon" name="epsilon"><small>Exploración: más alto prueba acciones menos conocidas.</small></label><label>Probabilidad de anomalías <b>{{config.anomalyProbability|number:'1.2-2'}}</b><input type="range" min="0" max="1" step="0.05" [(ngModel)]="config.anomalyProbability" name="anomalyProbability"><small>Frecuencia esperada de eventos maliciosos simulados.</small></label><label>Dificultad<select [(ngModel)]="config.environmentDifficulty" name="environmentDifficulty"><option value="EASY">EASY</option><option value="MEDIUM">MEDIUM</option><option value="HARD">HARD</option></select><small>Controla qué tan ambiguos o exigentes son los eventos.</small></label><button class="btn apply" type="submit" [disabled]="simulationLoading || !validConfig()">Aplicar configuración</button></form></article><article class="card train-card"><h3>Entrenamiento por lotes</h3><p class="muted">Prepara ejecuciones comparables con la configuración actual. Usa reset para iniciar un caso desde cero.</p><div class="train-actions">@for(n of [10,100,500,1000];track n){<button class="btn secondary compact" (click)="train(n)" [disabled]="simulationLoading">Entrenar {{n}}</button>}<button class="btn danger compact" (click)="reset()" [disabled]="simulationLoading">Resetear</button></div><div class="save-block"><h3>Guardar experimento actual</h3><p class="muted">Guarda configuración, métricas, Q-table y política aprendida del estado actual.</p><form class="save" (ngSubmit)="save()"><input [(ngModel)]="name" name="name" placeholder="Nombre del experimento" required><button class="btn" [disabled]="!name.trim()||experimentsLoading">{{experimentsLoading ? 'Guardando...' : 'Guardar experimento'}}</button></form></div></article></div>@if(experimentsLoading || simulationLoading){<p class="notice loading panel">Sincronizando con el backend.</p>}@if(success){<p class="notice success panel">{{success}}</p>}@if(error){<p class="notice error panel">{{error}}</p>}@if(state){<div class="metrics-grid panel"><app-metric-card label="Episodios" helper="Experiencias procesadas" [value]="state.metrics.episodes"/><app-metric-card label="Accuracy" helper="Decisiones correctas" [value]="formatAccuracy(state)"/><app-metric-card label="Recompensa media" helper="Promedio acumulado" [value]="formatAverageReward(state)"/><app-metric-card label="Falsos positivos" helper="Tráfico normal bloqueado" [value]="state.metrics.falsePositives"/><app-metric-card label="Falsos negativos" helper="Anomalías permitidas" [value]="state.metrics.falseNegatives"/></div><div class="grid two panel learning-grid"><app-q-table [entries]="state.qTable"/><app-policy-summary [policy]="state.learnedPolicy"/></div>}<div class="grid two panel"><article class="card"><h3>Cómo usar esta sección</h3><ol class="steps"><li>Ajusta alpha, gamma, epsilon, anomalías y dificultad.</li><li>Aplica la configuración.</li><li>Entrena por lotes para generar un caso comparable.</li><li>Guarda el experimento actual.</li><li>Compara resultados en la tabla.</li></ol></article><article class="card"><h3>Comparación de experimentos</h3><ul class="guide"><li>Accuracy alto indica mejores decisiones generales.</li><li>Falsos positivos son eventos normales bloqueados.</li><li>Falsos negativos son anomalías permitidas.</li><li>Recompensa total compara desempeño acumulado.</li><li>Epsilon alto suele generar más exploración y resultados más variables.</li></ul></article></div><article class="card panel"><div class="section-head"><div><h3>Experimentos guardados</h3><p class="muted">Compara precisión, recompensa, errores y configuración usada.</p></div><span class="pill">{{experiments.length}} guardados</span></div><div class="table-wrap"><table><thead><tr><th>Nombre</th><th>Episodios</th><th>Alpha</th><th>Gamma</th><th>Epsilon</th><th>Accuracy</th><th>Recomp. total</th><th>Recomp. prom.</th><th>FP</th><th>FN</th><th>Dificultad</th><th>Fecha</th><th></th></tr></thead><tbody>@for(e of experiments;track e.id){<tr><td><b>{{e.name}}</b></td><td>{{e.metrics.episodes}}</td><td>{{e.config.alpha|number:'1.2-2'}}</td><td>{{e.config.gamma|number:'1.2-2'}}</td><td>{{e.config.epsilon|number:'1.2-2'}}</td><td>{{e.metrics.accuracy|number:'1.1-1'}}%</td><td>{{e.metrics.totalReward|number:'1.0-0'}}</td><td>{{e.metrics.averageReward|number:'1.2-2'}}</td><td>{{e.metrics.falsePositives}}</td><td>{{e.metrics.falseNegatives}}</td><td>{{e.config.environmentDifficulty}}</td><td>{{e.createdAt|date:'short'}}</td><td><button class="btn danger compact" (click)="remove(e.id)" [disabled]="experimentsLoading">Eliminar</button></td></tr>}@empty{<tr><td colspan="13" class="empty">Aún no hay experimentos guardados.</td></tr>}</tbody></table></div></article></section>`,
  styles: [`.panel{margin-top:20px}.section-head{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;margin-bottom:16px}.amber{background:rgba(217,119,6,.12);color:var(--amber)}.config-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}.config-grid label{display:grid;gap:6px;font-weight:900;color:var(--navy)}.config-grid b{color:var(--teal)}.config-grid small{color:var(--muted);font-weight:700;line-height:1.35}.config-grid input[type=range]{accent-color:var(--teal)}.config-grid select,.save input{border:1px solid var(--line);border-radius:14px;background:#fffefa;color:var(--navy);padding:10px 12px;font-weight:800}.apply{align-self:end;background:var(--amber)}.train-card{display:grid;align-content:start}.train-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.save-block{border-top:1px solid var(--line);margin-top:20px;padding-top:18px}.save{display:grid;grid-template-columns:minmax(180px,1fr) auto;gap:10px;margin-top:14px}.metrics-grid{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:var(--space-xs)}.learning-grid{grid-template-columns:minmax(0,1.25fr) minmax(300px,.75fr);align-items:start}.steps,.guide{margin:12px 0 0;padding-left:20px}.steps li,.guide li{margin:8px 0}.compact{min-height:34px;padding:7px 12px;font-size:.84rem}.empty{text-align:center;color:var(--muted);padding:20px}@media(max-width:980px){.metrics-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.learning-grid{grid-template-columns:1fr}}@media(max-width:760px){.config-grid,.save,.metrics-grid{grid-template-columns:1fr}.section-head{display:grid}.compact{width:auto}}`]
})
export class ExperimentsComponent implements OnInit {
  experiments: ExperimentResponse[] = [];
  state: SimulationResponse | null = null;
  config: AgentConfig = { alpha: .2, gamma: .9, epsilon: .2, anomalyProbability: .3, environmentDifficulty: 'MEDIUM' };
  name = '';
  experimentsLoading = false;
  simulationLoading = false;
  error = '';
  success = '';

  constructor(private readonly experimentsApi: ExperimentService, private readonly simulation: SimulationService) {}

  ngOnInit() { this.loadState(); this.load(); }
  load() { this.experimentsLoading = true; this.error = ''; this.experimentsApi.list().subscribe({ next: r => { this.experiments = r; this.experimentsLoading = false; }, error: () => { this.error = 'No se pudieron cargar experimentos.'; this.experimentsLoading = false; } }); }
  loadState() { this.simulationLoading = true; this.error = ''; this.simulation.status().subscribe({ next: s => this.setState(s), error: () => { this.error = 'No se pudo cargar la configuración del agente.'; this.simulationLoading = false; } }); }
  applyConfig() { if (!this.validConfig()) return; this.simulationLoading = true; this.error = ''; this.success = ''; this.simulation.updateConfig(this.config).subscribe({ next: s => { this.setState(s); this.success = 'Configuración aplicada correctamente.'; }, error: () => { this.error = 'No se pudo aplicar la configuración.'; this.simulationLoading = false; } }); }
  train(episodes: number) { this.simulationLoading = true; this.error = ''; this.success = ''; this.simulation.train({ ...this.config, episodes }).subscribe({ next: s => { this.setState(s); this.success = `Entrenamiento de ${episodes} episodios completado.`; }, error: () => { this.error = 'No se pudo entrenar el agente.'; this.simulationLoading = false; } }); }
  reset() { this.simulationLoading = true; this.error = ''; this.success = ''; this.simulation.reset().subscribe({ next: s => { this.setState(s); this.success = 'Simulación reiniciada.'; }, error: () => { this.error = 'No se pudo resetear la simulación.'; this.simulationLoading = false; } }); }
  save() { if (!this.name.trim()) return; this.experimentsLoading = true; this.error = ''; this.success = ''; this.experimentsApi.save({ name: this.name.trim() }).subscribe({ next: () => { this.name = ''; this.success = 'Experimento guardado correctamente.'; this.load(); }, error: () => { this.error = 'No se pudo guardar el experimento actual.'; this.experimentsLoading = false; } }); }
  remove(id: number) { this.experimentsLoading = true; this.error = ''; this.success = ''; this.experimentsApi.delete(id).subscribe({ next: () => { this.success = 'Experimento eliminado.'; this.load(); }, error: () => { this.error = 'No se pudo eliminar el experimento.'; this.experimentsLoading = false; } }); }
  validConfig() { return ['alpha', 'gamma', 'epsilon', 'anomalyProbability'].every(k => { const v = this.config[k as keyof AgentConfig] as number; return v >= 0 && v <= 1; }); }
  formatAccuracy(state: SimulationResponse) { return `${state.metrics.accuracy.toFixed(1)}%`; }
  formatAverageReward(state: SimulationResponse) { return state.metrics.averageReward.toFixed(2); }
  private setState(state: SimulationResponse) { this.state = state; this.config = { ...state.config }; this.simulationLoading = false; }
}
